# To create a new controller, the name has to be exactly as the name of the file.
# Instead of having a "_" capitalize the second word and put all together.
# And inherits the ApplicationController class so all the funcionallity from it are available

class AboutController < ApplicationController
    def index
    end
end