<% if form.object.errors.any? %>
    <div class="container w-50 alert alert-danger text-start">
    <p>Error!</p>
        <% form.object.errors.full_messages.each do |message| %>
            <div>* <%= message %></div>
        <% end %>
    </div>
<% end %>